const evenNumbers = [2, 4, 6, 8]
const friends = ['sahoo', 'Abhi', 'Alok', 'manoj']

//Bessically indexOf() method returns the first index at which a given element can be found in the array, 
//-1 if it is not present.
evenNumbers.indexOf(6)
friends.indexOf('manoj')

console.log(evenNumbers.indexOf(6));
console.log(friends.indexOf('manoj'));